# Firmware Prototype

This firmware demonstrates a minimal ESP-NOW node and gateway.

Components:

Node: - reads sensor - sends ESP-NOW packet

Gateway: - receives ESP-NOW packets - forwards via USB serial
